#include "../../cub3d.h"

void	element(t_data *data, int fd)
{
	if ()
}

int	read_file(t_data *data, char *path)
{
	int	elem;
	int	fd;

	fd = open_file(path);
	if (fd < 0)
		;
	elem = 0;
	while (elem != 6)
	{

	}
}